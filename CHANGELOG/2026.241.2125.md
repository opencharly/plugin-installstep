# 2026.241.2125 — fix: bump sdk to v0.2026241.2117 for the machine-venue home-token fix

## Summary

Bumps `github.com/opencharly/sdk` to `v0.2026241.2117` (with the `spec` bump `go mod tidy`
pulls in) to deliver the machine-venue `{{.Home}}`-token fix to **install-step execution on machine venues**.

This plugin walks deploy plans through `kit.WalkPlans`. Until sdk v0.2026241.2117 that walk
never resolved the deferred `{{.Home}}` token on a machine venue, so a candy's
home-relative `copy: to: ${HOME}/...` reached `PutFile` carrying the literal token and
landed in a directory named `{{.Home}}` in the venue home — silently, with no error,
while `mkdir:`, `write:` and `command:` from the same candy applied normally (those legs
render shell that expands `$HOME` on the venue).

The fix itself is opencharly/sdk#188. **A plugin is built against its own pinned sdk**, so
each plan-walking plugin has to repin for its venue to get the fix — this one covers
any candy step routed through this plugin. Diff is `go.mod` + `go.sum` only.

Refs opencharly/charly#460.

## How tested

**Module gates on the committed tree:**

```
$ GOWORK=off go build ./...   ok
$ GOWORK=off go test ./...    ok
```

**The delivered behaviour is live-proven**, on the sibling venue where a disposable bed can
exercise it without touching a real machine. A minimal candy — one `mkdir:`, one `copy:`,
one `write:` into the same directory — applied to a disposable Arch cloud-image VM bed:

*Before* the sdk bump:

```
FAIL  the copied file landed alongside the written one  [copyrepro-copy-landed]  exit=1
$ sudo find / -xdev -name copied.txt
/home/arch/{{.Home}}/copyprobe/copied.txt      # the literal token became a directory
```

*After*, full `charly check run` including the fresh-rebuild leg:

```
3 steps: 3 passed, 0 failed, 0 skipped   EXIT=0
PASS  the copied file landed alongside the written one  [copyrepro-copy-landed]  exit=0
```

**Why the proof is on that venue and not this one:** the code delivered by this bump is
byte-identical (the same `kit.WalkPlans`), and exercising *this* plugin's venue means
deploying onto a real host/pod rather than a disposable guest. Running a `target: local`
deploy against the operator's own workstation to demonstrate a dependency bump would be a
destructive test for no additional information, so the shared seam is proven where it can
be proven safely.

## Rulebook compliance

- **R0 (skills)** — `/charly-internals:git-workflow` (branch/PR/landing) and
  `/charly-internals:plugin` (the plugin's sdk-pin contract) loaded before the change.
- **R1 (RCA every anomaly)** — the defect was root-caused before either change was
  written: the OCI target resolves the deferred token via `spec.ResolveHome`, the
  machine-venue walk had no equivalent. Evidence in opencharly/charly#460.
- **R2 (no "pre-existing" / "out of scope" split)** — nothing is deferred: the fix landed
  upstream (opencharly/sdk#188) and this repin delivers it. The sibling plugins that walk
  plans through the same seam are repinned in their own PRs, not parked.
- **R3 (no duplication)** — no code is added here at all; the fix lives once, in the sdk.
  The repin is how this module consumes it rather than carrying a local copy.
- **R4 (no workarounds)** — no pin override, no `replace`, no vendored patch: a released
  upstream tag consumed normally.
- **R4a (fix the product first)** — done in that order: the product fix is the sdk change;
  this is only its delivery.
- **R5 (hard cutover)** — N/A — nothing is deprecated or replaced; a version pin moves
  forward with no old path left behind.
- **R6 (git safety)** — `git status` inspected before staging (diff is exactly `go.mod` +
  `go.sum`); no force-push, no hook bypass, feature branch only.
- **R7 (prove the gate)** — module gates plus a bed run, pasted above; not a green
  `git status`.
- **R9 (deployed binary matches source)** — charly host-builds this plugin from the pinned
  candy source on connect, so the running provider is rebuilt from exactly this `go.mod`;
  the before/after bed pair demonstrates that rebuild changing behaviour.
- **R10 (fresh disposable proof)** — the bed evidence comes from a `charly check run`
  including its fresh-rebuild leg, from the committed tree.
- **RDD** — the one high-risk assumption (that the token, not the step, was the defect) was
  proved on a disposable VM before any code changed, by locating the delivered file at
  `/home/arch/{{.Home}}/copyprobe/copied.txt`.
- **ADE** — N/A — no candy authored here; the reproducer candy's `plan:` carries the
  acceptance checks and lives with the issue.
- **SDD** — N/A — no schema, CUE, or generated code touched.
- **Concurrency mandate** — N/A — a single serial dependency bump; nothing parallelisable.
- **Architecture gate / placement** — correct outermost layer: the behaviour fix belongs in
  the sdk seam every plan-walking plugin shares, and the only thing that belongs in this
  repo is the pin that consumes it. No core change.

## Change classification

- **Change class:** dependency bump (delivers an upstream bug fix); no source change here.
- **Verification gate:** `go build` / `go test` on the module, plus the before/after
  `charly check run` R10 pair proving the delivered sdk behaviour.
- **Attribution tier:** `analysed on a live system` — the delivered fix ran live with
  retained output on a disposable bed, but this plugin's own venue was not exercised (see
  above), so `fully tested and validated` would overstate it.

*Assisted-by: Claude Code claude-opus-5[1m] (analysed on a live system)*